# Build output
dist/
src-tauri/target/

# Dependencies
node_modules/

# Icons are generated by `npm run icons` but committed: the Rust build embeds
# them at compile time, so CI cannot build without them.

# OS and editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
